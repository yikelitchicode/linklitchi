# linklitchi

私人自用的 links hub / dashboard。

## 功能

- 搜尋 links
- 分類篩選
- tag 篩選
- 新增 / 編輯 / 刪除 link
- localStorage 保存
- JSON 匯入 / 匯出
- 可直接部署到 Vercel

## 本地打開

直接開 `index.html` 即可，或用任何靜態伺服器。

## 自訂

- 預設資料：`data/default-links.js`
- 樣式：`styles.css`
- 邏輯：`app.js`

## 部署

```bash
vercel --prod
```
